#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${package}.infrastructure.persistence.dao.TradeOrderMapper">

    <resultMap id="BaseResultMap" type="${package}.infrastructure.persistence.po.TradeOrder">
        <id column="id" property="id"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="String">
        INSERT INTO table(a,b,c) VALUES(${symbol_pound}{a}, ${symbol_pound}{b}, ${symbol_pound}{c})
    </insert>

    <update id="update" parameterType="String">
        UPDATE table SET a = ${symbol_pound}{a} WHERE b = ${symbol_pound}{b}
    </update>

    <select id="queryEmployeeByEmployNumber" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT a, b, c
        FROM table
        WHERE a = ${symbol_pound}{a}
    </select>

</mapper>
